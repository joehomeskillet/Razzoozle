# Razzoozle — Backlog für neue Session (Stand 2026-07-16)

**Twin:** `rust.razzoozle.xyz` · Rust-only :3012 · health-gated CD (`razzoozle-rust-cd.timer`, auto-rollback).
**Deployed == main == origin/github == `1ce243a0`** · healthz 200.
**Rolle:** Fable orchestriert (Konzept/SDD + Merge/Gate/Deploy), tippt NIE Produkt-Code. Write-Worker im eigenen Worktree, Reviewer ≠ Autor, cross-vendor. grok+codex 50/50 als Primär-Write-Lanes (User-Direktive 2026-07-15), sich gegenseitig prüfen; agy als Zweitmeinung; sonnet-worker für scoring/auth-kritische Kerne; Free-Pool nur Trivial.

## Standing-Regeln (jede Welle)
- **Gates vor Merge:** `bash rust/gate.sh` (GO) · `CI=true pnpm --filter @razzoozle/web run types` (GENAU 2 bekannte Fehler: `resolveIcon` configurations/index.tsx, `handleStatusChange` GameWrapper.test.tsx) · `pnpm --filter @razzoozle/web run build` · `bash scripts/check-locales.sh` (LOCALES OK). Rust/Locale nur gaten wenn berührt.
- **Deploy = Push:** `origin/main` → CD baut Image (dist wird gebacken + nach `.web-dist-live` rsynct) → healthz-Gate. Vor jedem Push `git fetch origin main` (Auto-Prozess `agent@joelduss.xyz` pusht design-sync auf main, nur `.design-sync/`, kein Live-Code).
- **Nach jeder Welle:** deploy + **Live-Browser-Test des vollen Loops** (login→create→START→play→reveal→finish→podium). Interaktions-/Timing-Bugs findet nur der echte Browser — Code-Review reicht nicht (Lektion #76, Memory `auto_autoadvance_fullscreen_hover_gate`). Admin-PW: `docker exec razzoozle-rust env | grep BOOTSTRAP_ADMIN_PASSWORD`.
- **Split-Check je WP** (1 WP ≈ 1 Datei <150 LOC; Tests/CLI/Docs eigene WPs). **WP-Issue-Trail** in Gitea agent-claude/Razzoozle.

---

## Welle A — Player-facing Bugs (höchster Wert, klein) — ZUERST
| # | Titel | Scope / Pointer | Lane |
|---|---|---|---|
| **#30** | Solo-Link mit aktiver Manager-Session redirected still nach /manager/config | Solo-Route-Guard: aktive Manager-Session darf Solo-Link nicht kapern. Repro-first (Client-Router-Redirect-Logik). | codex |
| **#32** | Wortarten-Fragen bei Quick-Play still gestrippt (kein Manager-Hinweis) | Quick-Play strippt nicht-unterstützte Fragetypen ohne UI-Hinweis → sichtbare Warnung/Badge. 3 Kommentare (Kontext lesen). | grok |
| **#83** | Lobby-Spieler verliert Slot bei >10s Sperre VOR Spielstart (ShowRoom hard-remove) | Server: `rust/server/src/state/eviction.rs` ShowRoom-Branch (~Z.117-125) hart-remove → mit Grace behalten wie mid-game (~Z.126-132). Reconnect-Fix #77 deckt danach auch die Lobby. | grok/sonnet |
| **#84** | Stale-Roster: entfernter Lobby-Spieler bleibt beim Manager „joined" | Paart mit #83: beim Hard-Remove `player:left`/Roster-Update an Manager emittieren. Client-Roster in Manager-Lobby. | codex |

DoD: alle 4 live browser-verifiziert (Solo-Link öffnet Solo trotz Manager-Login; Wortarten-Strip zeigt Hinweis; Lobby-Sperre >10s reconnected nahtlos; Roster aktualisiert).

## Welle B — Editor/Navigation UX-Bugs (klein)
| # | Titel | Scope | Lane |
|---|---|---|---|
| **#27** | Stray-Navigation aus Quiz-Listen-Controls in den Editor (Event-Bubbling) | `stopPropagation` auf Listen-Control-Buttons; Editor-Nav nur bei echtem Karten-Klick. | grok |
| **#28** | Quiz-Editor False-Dirty — beforeunload ohne Änderung | Dirty-Flag nur bei echter Änderung setzen (Initial-State-Vergleich). | codex |

DoD: keine ungewollte Editor-Navigation; kein beforeunload-Prompt ohne echte Änderung.

## Welle C — Test-Infrastruktur (Regression-Netz, mittel)
| # | Titel | Scope | Lane |
|---|---|---|---|
| **#35** | WP-SH0/SH1/SH2 Stagehand-Testwelle | Scaffold (`source/e2e`, outside pnpm-workspace → `--ignore-workspace` + `npx tsx`, Memory `reference_e2e_dir_outside_pnpm_workspace`) + MP-Loop-Spec + Solo-Matrix-Spec (3 Viewports/Fragetyp, Memory `feedback_e2e_solo_coverage`). | sonnet + /stagehand |

DoD: `mp-loop` + `solo-types` grün gegen rust.razzoozle.xyz; act-cache für LLM-freie Re-Runs. **Empfehlung: vor Welle D**, damit Feature-Wellen Regression-abgedeckt sind.

## Welle D — Features
| # | Titel | Scope | Lane |
|---|---|---|---|
| **#14** | WP-L5 Katalog-Fläche Label-Chips + Assign + Filter (Labels Wave 2) | Katalog-Labels: Chips + Assign-UI + Filter. `labels-sdd-2026-07-14.md` als Basis. frontend-design-Spec zuerst (Layout-Entscheidungen). | frontend-design → grok/codex |
| **#31** | WP-I18N Sammel-Fix (common-Keys, es/game.json, hardkodierte DE, create-Capitalization) | Locale-only, 6 Namespaces ×6 Sprachen. NIE textuell mergen — `scripts/locale-sync.mjs` / `@locale-sync`. Gate `check-locales.sh`. | locale-sync |

## Welle E — Security/Hardening + Cleanup (kein kritischer Pfad)
| Item | Scope | Lane |
|---|---|---|
| **#49** | DoS-Guards für solo-score Endpoint (aus SEC-05-Review) | Rate-/Struktur-Cap auf solo-score. Call-Site-Grep-Beweis (Memory `security-wiring-proof`). | codex |
| **SEC-M1 tx-wrapping** (kein Issue) | Session-Revocation (`db/users.rs` `revoke_user_sessions`) + PW-Update in EINE Transaktion wrappen (aktuell 2 separate Statements). | grok |
| **5 ai.ts-Validatoren** (kein Issue) | Verwaiste Request-Validatoren `packages/common/src/validators/ai.ts` (~30 LOC) — re-gate, dann löschen. | free-pool |
| **lifecycle W2-31/32** (kein Issue) | Refactor Schritte 4-5 + `state/reveal.rs` (Barrel-Split-Fortsetzung), kein Audit-Item. | grok |
| **Residual edge-grays** (kein Issue) | Rest-Grays außerhalb der 411 aus Welle 1 (Icons/Rand) → Ink-Tokens. | free-pool |

---

## Reihenfolge (Empfehlung)
A (Player-Bugs, small fan-out ≥3) → Deploy+Test → **C (Stagehand-Netz)** → B (Editor-UX) → D (Features, frontend-design-Spec zuerst) → E (Hardening/Cleanup, Overflow). Jede Welle: eigener Deploy + Full-Loop-Browser-Test.

## Verweise
- Gameplay-Bug-Wave dieser Session: HANDOFF.md (oben), Issues #76/#77 (closed).
- Ältere SDDs: `audit-followup-sdd-2026-07-15.md`, `labels-sdd-2026-07-14.md`, `security-wave-sdd-2026-07-15.md`, `i18n-guard-sdd-2026-07-15.md`.
- Security-Audit-Basis: `docs/security/rust-razzoozle-security-audit-2026-07-13.md`.
