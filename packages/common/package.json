{
  "name": "@razzoozle/common",
  "type": "module",
  "scripts": {
    "test": "vitest run",
    "types": "tsc --noEmit",
    "format": "prettier --check . --log-level warn",
    "format:fix": "prettier --write . --log-level warn"
  },
  "dependencies": {
    "zod": "^4.4.3"
  },
  "devDependencies": {
    "@types/node": "^25.6.2",
    "prettier": "^3.8.3",
    "socket.io": "^4.8.3",
    "typescript": "^6.0.3",
    "vitest": "^4.1.7"
  }
}
