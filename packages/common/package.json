{
  "name": "@razzoozle/common",
  "type": "module",
  "scripts": {
    "test": "vitest run",
    "types": "tsc --noEmit",
    "format": "prettier --check . --log-level warn",
    "format:fix": "prettier --write . --log-level warn"
  },
  "dependencies": {
    "zod": "^4.4.3"
  },
  "devDependencies": {
    "@types/node": "^26.1.0",
    "prettier": "^3.9.4",
    "socket.io": "^4.8.3",
    "typescript": "^6.0.3",
    "vitest": "^4.1.9"
  }
}
